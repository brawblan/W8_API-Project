both slider buttons go same direction (only watchList is working, copy or find a way to get both on one JS file)

when I remove a card from a list, can I make the other cards change classes to active and next?

reset filter to what it was on before clicking list